# file-organiser-go

Simple app to automatically move files between directories (orgaisation purposes).

## How to use

Run with go:
```bash
go run . --config="/path/to/file_org_cong.yaml"
```

Example of config can be found in `config.example.yaml` in the root of the repo.
